# PAAR-aicoding-lite 工作入口

只默认读取本文件、work/current.json 和与当前业务结果直接相关的文件。
历史研究副本不属于本项目的活动规则，也不进入公开提交。

- 用户讲业务；你负责技术实现。先核对原意，区分事实、假设与未知。
- 当前任务的目标、范围、状态、决策、验收只维护在 work/current.json。
- PAAR 是内部循环，不要求逐阶段登记；已有授权范围内不重复询问。
- 简单任务不建立候选卡、正式卡、派发记录、文件锁表或技能路由报告。
- 仅在业务效果、成本、权限、不可逆操作或验收标准变化时向用户提问。
- 多闭环需求才按需读 docs/r5.md；整体验收不能用局部测试替代。
- 修改前检查实际 Git 状态，保护既有修改。默认单写入者，先做最小变更。
- 决策和证据可检查；工具返回“完成”不是业务完成证明。
- 运行 node scripts/paar.mjs check 核对记录；产品代码变更还需实际测试。
- 同类修复两次无新诊断证据，停止继续写入，保留失败和下一诊断动作。
- 验证通过只可进入 awaiting_acceptance；用户明确验收后才标 done 并记录 accepted_by。
- 不伪造确认人或证据。记录校验不能证明证据内容真实、代码正确或权限隔离。
- 发布、删除、安装、权限扩大等动作遵守当前用户的明确授权。
